using UnityEngine;
using UnityEngine.AI;

public class TestMove : MonoBehaviour
{
    public Transform targetBase; // 거점

    void Start()
    {
        // 시작하자마자 무조건 거점으로 뛰어라!
        GetComponent<NavMeshAgent>().SetDestination(targetBase.position);
    }
}
